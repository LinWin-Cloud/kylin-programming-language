public class API {
    public static Boolean apiIs(String code) {
        int length = code.length();
        for (int i =0;i<code.length();i++) {
            if (code.substring(code.indexOf(""),i).equals("api."))
            {

            }
        }
        return true;
    }
    public static void API_run() {

    }
}
